version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install streamlit boto3
run:
  command: streamlit run app.py --server.port 8080 --server.address 0.0.0.0
  network:
    port: 8080
